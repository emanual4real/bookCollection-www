/**
 * Represents a book.
 * @constructor
 */
function book(name, type, value, className, sort, order, visible) {
    name,
    type,
    value,
    className,
    sort,
    order,
    visible
}

export default book;